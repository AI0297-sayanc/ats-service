<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1.0">
    <meta property="og:title" content="<%= opening.title %>">
    <meta property="og:description" content="<%= opening.shareableText || 'Recruitech' %>">
    <meta property="og:image" content="<%= opening._organization.logoUrl %>">
    <meta property="og:url" content="<%= `${process.env.SHARE_OPENING_BASE_URL}/opening/${opening._id}`  %>">
    <script src="<%= `${process.env.WIDGET_SRC_URL}/js/app.js` %>"></script>
    <link rel="stylesheet" href="<%= `${process.env.WIDGET_SRC_URL}/css/app.css` %>" />
    <title>recruitech-widget</title>
  </head>
  <body>
    <noscript>
      <strong>We're sorry but recruitech-widget doesn't work properly without JavaScript enabled. Please enable it to continue.</strong>
    </noscript>
    <h1>
    <img src="<%= opening._organization.logoUrl %> " style="max-width: 48px; max-height: 48px;" />&nbsp;&nbsp;
      <%= opening._organization.title %>
    </h1>
    <hr/>
    <!-- <div id="app"></div> -->
    <recruitech-widget api-key="<%= opening._organization.widgetApiKey %>" opening-id="<%= opening._id %>"></recruitech-widget>
    <!-- built files will be auto injected -->
  </body>
</html>
